
<h1><%= @blog.title %></h1>
<%= link_to "Edit", edit_blog_path(@blog) %>
<%= link_to "Delete", @blog, method: :delete, data: { confirm: "Are you sure?" }%>
<%= link_to 'Back', blogs_path %>
<%= link_to 'posty', blog_posts_path(@blog)%>

<%# @posts.each do |post| %>
<!--  <h3><%#= post.title%></h3>-->
<%# end %>